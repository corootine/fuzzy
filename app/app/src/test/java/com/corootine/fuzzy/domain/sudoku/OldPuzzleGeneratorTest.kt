package com.corootine.fuzzy.domain.sudoku

import org.junit.Test

class OldPuzzleGeneratorTest {

    @Test
    fun generatePuzzle() {
//        val boardGenerator = BoardGenerator()
//        val puzzleGenerator = PuzzleGenerator()
//
//        val board = boardGenerator.generateRecursively() // internal to the puzzle generator?
//        val puzzle = puzzleGenerator.generate(11543)
//
//        // 1. compare the puzzle against board -> would result in a "unique" solution
//        // 2. do manual check for every insert -> can lead to multiple solutions in case of a not well-formed puzzle -> that's OK
//
//        val puzzleSolver = PuzzleSolver(puzzle)
    }
}